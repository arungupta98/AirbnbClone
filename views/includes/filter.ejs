<div id="filters">
  <div class="filter-controls">
    <button class="filter-btn" id="filterBtn">
      <i class="fa-solid fa-sliders"></i>
      <span>Filters</span>
    </button>
    <div class="tax-toggle">
      <div class="form-check-reverse form-switch">
        <input
          class="form-check-input"
          type="checkbox"
          role="switch"
          id="switchCheckDefault"
        />
        <label class="form-check-label" for="switchCheckDefault"
          >Display total after taxes</label
        >
      </div>
    </div>
  </div>
  <div class="filter-dropdown" id="filterDropdown">
  <div class="filter">
    <form action="/listings" method="GET">
      <button type="submit" name="category" value="trending">
        <i class="fa-solid fa-fire"></i>
        <p>Trending</p>
      </button>
    </form>
  </div>
  <div class="filter">
    <form action="/listings/filter" method="GET">
      <button type="submit" name="category" value="rooms">
        <div>
          <i class="fa-solid fa-bed"></i>
        </div>
        <p>Rooms</p>
      </button>
    </form>
  </div>
  <div class="filter">
    <form action="/listings/filter" method="GET">
      <button type="submit" name="category" value="iconic cities">
        <div>
          <i class="fa-solid fa-mountain-city"></i>
        </div>
        <p>Iconic cities</p>
      </button>
    </form>
  </div>
  <div class="filter">
    <form action="/listings/filter" method="GET">
      <button type="submit" name="category" value="mountain">
        <div>
          <i class="fa-solid fa-mountain"></i>
        </div>
        <p>Mountain</p>
      </button>
    </form>
  </div>
  <div class="filter">
    <form action="/listings/filter" method="GET">
      <button type="submit" name="category" value="castles">
        <div>
          <i class="fa-brands fa-fort-awesome"></i>
        </div>
        <p>Castles</p>
      </button>
    </form>
  </div>
  <div class="filter">
    <form action="/listings/filter" method="GET">
      <button type="submit" name="category" value="pools">
        <div>
          <i class="fa-solid fa-person-swimming"></i>
        </div>
        <p>Amazing Pools</p>
      </button>
    </form>
  </div>
  <div class="filter">
    <form action="/listings/filter" method="GET">
      <button type="submit" name="category" value="camping">
        <div>
          <i class="fa-solid fa-campground"></i>
        </div>
        <p>Camping</p>
      </button>
    </form>
  </div>
  <div class="filter">
    <form action="/listings/filter" method="GET">
      <button type="submit" name="category" value="farms">
        <div>
          <i class="fa-solid fa-cow"></i>
        </div>
        <p>Farms</p>
      </button>
    </form>
  </div>
  <div class="filter">
    <form action="/listings/filter" method="GET">
      <button type="submit" name="category" value="artics">
        <div>
          <i class="fa-regular fa-snowflake"></i>
        </div>
        <p>Arctic</p>
      </button>
    </form>
  </div>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function () {
    const filterBtn = document.getElementById('filterBtn');
    const filterDropdown = document.getElementById('filterDropdown');

    // Toggle dropdown on button click
    filterBtn.addEventListener('click', function () {
      filterDropdown.classList.toggle('active');
    });

    // Close dropdown when a filter is clicked
    const filterButtons = document.querySelectorAll('.filter button');
    filterButtons.forEach(button => {
      button.addEventListener('click', function () {
        filterDropdown.classList.remove('active');
      });
    });

    // Close dropdown when clicking outside
    document.addEventListener('click', function (e) {
      if (!filterBtn.contains(e.target) && !filterDropdown.contains(e.target)) {
        filterDropdown.classList.remove('active');
      }
    });
  });
</script>
